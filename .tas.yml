---
framework: jest
preRun:
     command:
        - yarn install
preMerge:
  pattern:
    - "./packages/*/src/__tests__/*.spec.ts*"
postMerge:
  pattern: 
    - "./packages/*/src/__tests__/*.spec.ts"
postRun:
     command:
        - yarn --version
version: 1.0
